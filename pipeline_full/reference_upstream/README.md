Large development-output artifacts (OOF predictions, per-model test predictions, candidate submission CSVs, Optuna search databases, logs, and intermediate handoff packages) are not included in this review repository. The final saved model-level outputs required for deterministic slot1 verification are included under pipeline_full/runnable/cached_intermediates/. This directory provides source code and methodological provenance for the two model tracks; it is not a bit-exact raw-to-submission retraining path.
