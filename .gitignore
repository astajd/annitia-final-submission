# Runtime outputs from the smoke test
pipeline_full/runnable/_outputs/
pipeline_full/runnable/generated_slot1_prediction.csv

# Generated by retrain_all_from_raw.sh (working tree + outputs) — not committed
pipeline_full/runnable/retrain_work/
pipeline_full/runnable/retrain_outputs/

# Local virtual environments
pipeline_full/runnable/.venv/
.venv/
venv/

# Raw challenge data — ignore the directory contents by default, but allow the
# four organizer-provided files to be committed into the PRIVATE review repo
# (with organizer permission). retrain_all_from_raw.sh requires these at data/raw/.
data/raw/*
!data/raw/train.csv
!data/raw/test.csv
!data/raw/dictionary.csv
!data/raw/hello_world_submission.csv

# Python
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# Local environment files
.env

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/